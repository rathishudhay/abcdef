package com.steelkiwi.cropiwa.PhotoEditor.MainCode.filters;

import com.steelkiwi.cropiwa.PhotoEditor.PhotoFilter;

public interface FilterListener {
    void onFilterSelected(PhotoFilter photoFilter);
}